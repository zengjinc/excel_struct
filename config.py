# Configuration file for excel_struct

# Target folder for generated files
# Default: 'target' directory in the current working directory
# 支持相对路径和绝对路径
TARGET_FOLDER = './target'

# Struct folder for description files
# Default: 'struct' directory in the current working directory
# 支持相对路径和绝对路径
STRUCT_FOLDER = './struct'
